# Root preemption test scope and explicit historical-fixture migration

This is a test-only prerequisite to the next root/publication repair. Production remains f7c49c0a. Do not call the normalizer change source-equivalent whole-root coverage.

## Actual initial execution

The first added root-test commit eab8a163 did not compile Wholesome because the new test omitted two namespaces. The imports-only follow-up 22f8870e compiled all seventeen integrated entries; sixteen ran successfully and Wholesome retained assertion failures. Integrated run34894920587, artifact10368307046, authenticated SHA256 df88ed0ab862281730e37ecccd567e33398c79f38e4288bc1829dbec46f2b458, records root6/27 with21 assertions/0unexpected, quest-action8/25 with17 assertions/0unexpected, executor23/23 and navigation16/16. These are not21 independently established bugs. One new instance-isolation case failed its setup before reaching the ownership oracle: an actual second publication replaces QuestOrder, so its controlled behavior must be installed after that publication. This test-only reconciliation makes that installation explicit. The same-path reload case is strengthened with a second actual driver cycle so a one-cycle executor replacement veto cannot mask missing continuing-publication authorization. No production defect is attributed to the import/setup mistakes.

## Seven historical isolated-gate fixtures

Program.cs and the QuestSchedulerRawPublication, QuestPublicationAcceptance, QuestPublication, QuestScanEntry, QuestScanFailure and QuestScanRevocation groups each replace the complete Bot.Root child with a controlled leaf. Several also seed LastSchedule via reflection without performing a real host publication. Their safety oracles are valuable tests of schedule policy plus WholesomeExecutionGate, but they cannot simultaneously specify a complete root that must keep combat/service reachable during quest denial.

The opt-in LegacyScheduleGateFixture and Tools/W42TestAggregation/isolate_schedule_gates.py now make that existing isolation explicit at normalization time. Exactly one factory expression in each of the seven named files is replaced by construction of the actual production WholesomeExecutionGate with the existing production rest/schedule policy. The counted leaves, observations, transitions, cancellation checks and every assertion stay intact. Original source bytes are retained, and the manifest records each before/after generated hash, exact expression and explicit isolated-gate scope. Unknown, missing or duplicate source contracts fail generation rather than dropping coverage. This is a declared fixture-topology migration, not a production test hook or assertion weakening. The ordinary aggregate initializer transformation remains separate.

QuestActionFreshnessRegressionTests and QuestRootPreemptionRegressionTests are excluded from that migration and continue using actual Bot.Root, real scheduler publication/profile load and actual ForcedBehaviorExecutor. The initial27-case group, now extended to31 with actual OnTick/exclusivity observation-change and replacement-publication controls, uses the real driver contract (Start only when not Running) and counts nested cleanup before protective effects, no roaming, service exclusivity, per-instance graphs, cancellation and reentrant replacement. Controlled support/terminal leaves do not establish native game effects.

The six new Python migration utility tests were authored first, initially failed because the module was absent, and then passed with the eight retained normalizer tests. These fourteen local utility checks are not C# owner execution. Require a fresh exact-source Windows red with this complete test-only revision before production implementation, then byte-identical original/generated fixtures across the repair. Preserve all historical evidence and keep PR44 draft/unmerged.
