# Root repair review and test-only reconciliation

The published candidate is `229f44efc03b930d54f9e2e4a18253ecf9fe1177`, tree `73c020dfe576cb77c6dfaa82292674d671ef52cc`. Its focused Windows x86 run34901633338/current artifact10370264073 has SHA256 `7bc689a7216500cf996719a2b19f24fbbe0b7687226aa02dc4a2571ed7c31f41`. All four focused builds pass. QAF25/25, EO23/23 and navigation16/16 pass; root28/31 has three assertions, and raw-publication21/23 has two unexpected reflection parameter-count errors. Wholesome remains run1. These are actual candidate failures, not final acceptance.

## Distinguish the observed causes

The new root31 fixture borrows a raw-publication fixture which zeroes unrelated descriptor fields. It replaces death support with a Failure leaf but never supplies Health. Actual `WoWUnit.Dead` is `CurrentHealth == 0`. Consequently its normal-running, valid-exclusive and pending-refresh controls simultaneously describe a dead player and a disabled death branch. Candidate preemption observes that persistent death state and stops/restarts the quest. Do not remove death protection to satisfy this contradictory setup. This test-only amendment writes Health=100 and asserts IsAlive/non-ghost before the real scan; all 31 existing case names and assertions remain unchanged.

The two raw-publication exceptions have a different cause: the candidate changed the retained nonpublic four-argument ScanAndRefresh method into a five-argument method. Optional arguments do not preserve MethodInfo.Invoke parameter count. Restore the old method contract in production and give the explicitly owned five-argument entry a distinct name. Do not change the original reflection fixtures to hide this regression.

## Review-driven tests before further production changes

`QuestRootProtectionRegressionTests` adds seven cases using the existing actual root/publication/executor fixture. It exercises actual death/ghost descriptor changes and a real WoWUnit pet registered with ObjectManager, with allocated test-process object/descriptor storage and state restoration. Pet health/combat/identity are independently asserted before the support oracle. Support remains a controlled counted leaf; native combat actions and a game client are not exercised. Current LevelBot combat admission includes a living pet in combat; the candidate root's protective-priority observation omits it. Alive/idle and dead-pet controls must prevent unnecessary lifetime resets.

`QuestCleanupSignalRegressionTests` adds fourteen actual Composite/GroupComposite stop cases. It checks exception identity, handler and iterator disposal counts before repeated Stop, cancellation/interruption versus ordinary handler/sibling/disposal failures, and first-stop-signal precedence. Source review found that older cleanup aggregation promotes ThreadInterruptedException but not OperationCanceledException, and an iterator-finally exception can replace an earlier cleanup signal. These are hypotheses until the new exact-source Windows red runs; no predetermined pass count is assigned.

The three existing production repairs and all other original tests/workflows are retained. This commit is tests and documentation only. Do not call these additions repaired before execution. The two raw-publication compatibility failures remain visible until the production correction. Independent review was requested through the native reviewer action, but follow-up requested-reviewer reads were empty; no assignment or approval is claimed. Self-review is not independent approval. No master/backup write, merge, deployment, installed-bot/native/mesh/Lua change or original-client acceptance is authorized by this amendment.
