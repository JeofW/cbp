# Continue W42 after actual PR #40 execution

Continue the evidence-led CopilotBuddy audit in `JeofW/CopilotBuddy-private`. Do not restart the broad audit or recreate existing repairs. Read this newer reconciliation before older ZIP prompts.

## Execution is proven, not merely permitted

The preceding aggregation session successfully used authenticated GitHub branch/file writes, created draft PR #40, and triggered and inspected real Windows x86 .NET 10 runs through authorized pushes. Its connector exposed writers despite older sessions' read-only action sets. Local Git still failed DNS and no local C# runtime existed; C# execution was on GitHub Windows runners. Verify this session's actual actions once; do not assume either capability or incapability from a previous session. Do not misuse GET endpoints, expose credentials, or substitute permission metadata for a successful write. If an execution gate really is unavailable after one bounded check, stop rather than conduct another extended read-only audit.

Inspect current master, backup, W42 branch heads and open PRs. Preserve master `8382a7ec05a64212ea0a237159dca427a0767425` and backup `audit/backup-master-before-approved-merge-20260913` at `c43c50d8d5d6775055f19bf018b52930a264d4a4`. PR #34 is already merged; do not remerge old stacks. PR #25 remains excluded. No merge, deployment, installed-bot change, Navigation.dll replacement, runtime capture or mesh download without separate authorization.

## Existing remote work to preserve

- Original test-only W42 branch `audit/next-42-quest-log-snapshots-20260914` at `2d3728feb22938428fe64d8ff8204d5779cb4e9e`.
- Draft #38 `audit/next-42-observation-boundary-20260914` at `3792caae261db595836eea794128c4305a241cbb` added the extracted boundary and full completion fixtures.
- Draft #39 `audit/next-42-completion-owner-20260914` last read at `318c0a7d9e8518a468a52af05134aaad3e401eee`. Its production repair is `1dccead847f330430f148609858c6f89d5724ad8`: raw accepted cache misses return accepted/Unknown instead of borrowing history. Do not recreate it. Read its W42_COMPLETION_OWNER_CHECKPOINT.md and evidence/graph files. Later changes include a supplemental retained baseline and documentation, not additional production repair.
- Draft #40 `audit/next-42-test-aggregation-20260914` starts from 1dccead8. Final tested production/test/workflow commit is `cd82129ec81f8e51fa12620f95b307e2ca5b34b6`, tree `36f2c1065c678644460a4f15a3152de00c51c9b4`; subsequent W42_AGGREGATION_* checkpoint additions are documentation only. Compare the current head against cd82129 before inheriting evidence. Base #39 advanced independently; do not overwrite, force-push or silently merge its changes.

Read all four W42_AGGREGATION_{CHECKPOINT.md,EVIDENCE.json,GRAPH.json,NEXT_CHAT.md} files on #40. The preserved uploaded ZIP remains local historical preparation, not a committed repair. Preserve #35 typed relations and #36 accepted/scheduled/shared/consumable protection, already inherited. W41 handover is #37 / 0e2c092b23514ffd530a9d14d0270a9b1dcca0d7; retain newer corrections rather than obsolete pre-merge instructions.

## Verified frontier — do not repeat aggregation work

#40 normalizes test execution after module initialization using generated copies under obj, preserving every original assertion. Both normalized variants now execute all 25 main, five ready-log and five scheduler cases. All nine Wholesome deferred groups start, later groups and original Main finish, and a failing scheduler group still leaves process exit 1. Do not remove red cases or replace the original 23-case sale suite.

Final normalized comparison run 34794252845: baseline artifact 10329485658, current artifact 10329646073. Actual integrated run 34794252860: artifact 10329172220. Exact SHA-256 digests are in W42_AGGREGATION_EVIDENCE.json. Complete artifacts were downloaded and verified. Current focused and integrated runs share cd82129 and all 1,672 recorded input hashes; 21 normalized files/manifests are identical between baseline and current.

Results: full completion owner 10/15 on unchanged baseline -> 15/15 with inherited repair, zero unexpected errors. Extracted boundary 13/24 -> 16/24, remaining eight assertions. Main observation 3/25 -> 4/25, with two remaining sale assertions and 19 missing proposed CaptureSnapshot contracts. Ready-log remains one control pass/four assertions. Scheduler remains one empty-log control pass, one null-input pickup assertion, two missing HasCompleteQuestLog contracts and one unresolved NullReferenceException. Missing contracts and fixture/runtime exceptions are NOT reproduced production defects; overlapping suite counts are not unique bugs.

Integrated: all original 14 entries plus three W42 entries execute, all build exits 0, 14 run exits 0 and three run exits 1. Original sale 23/23, relation identity 23/23, analyzers 33/33, Singular 99 source files. The combined result remains red. The first integrated attempt at 678c6d0 ran all entries but failed afterward in file hashing; its missing manifests were repaired with literal paths and both workflows rerun at cd82129. Preserve that distinction.

## Next bounded work

First isolate the existing scheduler unavailable-owner NullReferenceException and preserve its full exception/stack. Keep its intended assertion/control; do not call it a production reproduction until the real owner path is reached under a valid fixture. Normalize/reporting is already fixed; do not rebuild the test projects.

Then close successful occupied raw slot reads -> actual ScanAndRefresh -> MaterializeSchedule -> ProfileBuilder.WriteProfile -> DoScan/ProfileManager.LoadNew -> already-executing WholesomeExecutionGate. Preserve raw accepted IDs and slot identity independently of optional metadata, distinguish identity-read and metadata completeness, and carry real host player/session/observation provenance. Returning false from a future scan is not enough: invalidate stale already-executing permission and revalidate side effects.

Do not fabricate a reliable revision from names, counts, equal scans, PID or character/realm equality. ReadInternal<T> can return default when ReadBytes fails; zero alone is not proven empty. Default AcquireFrame is not automatically hard native capture. FrameLock captures an executor with release/timeout behavior that must be traced before claiming a lease. Equal scans cannot prove atomicity or detect A-B-A changes.

Use actual production owners, not a helper model, through completion/sale/scan/schedule/profile publication/executing-plan boundaries. Cover mid-scan read failure, hydration and eviction, full logs, same-count swaps, refresh waits, duplicate/invalid IDs, player/GUID/descriptor/memory/session replacement, same-character reconnect, cancellation and timeout. Unknown is not abandonment, completion, free capacity or permission to sell. Ready fixture outcomes are controlled refresh/history observations, not actual turn-ins or abandonment. Controlled merchant calls are not real item deletion or native/server acceptance.

Preserve public APIs and complete/empty normal behavior, #35 typed relations and all #36 protections. Keep current missing-contract cases honestly classified while implementing only owner-backed contracts. Require comparable baseline/repaired fixtures and actual affected plus all-17 integrated Windows x86 .NET10 runs on the same new source commit, with full artifacts, exact source hashes and every result inspected. Target original WoW3.3.5a build12340. No Lua changes without executable Lua5.1 checks. No managed-test mesh downloads; only required verified LFS tiles in a later authorized mesh slice.

The normalized workflow currently has a branch-specific push trigger for the #40 branch, not workflow_dispatch. Review that trigger when working on a new branch; use an actual authorized push or separately verified dispatch path. Do not infer execution from workflow YAML alone.

After W42, keep the saved dependency order: owner-scoped item protection; atomic FILE reload (FILE sets are cleared before parsing, runtime/profile sets are NOT cleared); merchant/mail/discard/consumption; vendor discovery/settings; rest/restart/cancellation; special quests; roster identity; combat/support; native/lift work. Do not jump to that backlog before observation/publication/dispatch dependencies are closed.

Publish only focused review PRs backed by actual writes and executable evidence, no merges. Save durable checkpoints after bounded slices. Distinguish source-confirmed, controlled-reproduced, regression-verified and live-unverified. Full W42 and the exhaustive architecture/refactoring audit remain unfinished.
