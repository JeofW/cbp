# Executor test reconciliation before production repair

Test-only commit3dbb79af ran on actual Windows x86: focused run34866295836/current artifact10356903039, SHA2560224a65d21a38ec61899376ad7a42ac19653730555467b902a765c073b50a3fc. All4 focused builds0, Wholesome run1 and other3 run0; new executor group5/23 with18assertions/0unexpected. ZIP CRC,55 internal file hashes, exact source identity and new test blob1e3e38e4d00bb29f7fa021f03f7d220eed127da4 were verified. The unchanged25-case quest-action group remains8/25,17assertions/0unexpected.

Before production work, the wider retained harness exposed a constraint: Tools/QuestRecoveryRegressionTests/Program.cs:1621-1629 explicitly requires a deferred ForcedBehaviorExecutor to remain a valid Running iterator across repeated ticks. The new initial-deferral Failure expectation was an incorrect compatibility assumption, not a newly discovered production defect. Do not call all18 assertions18 valid defects or18 unique bugs.

This test-only follow-up corrects only the newly authored deferral expectations. Initial deferral keeps the original Running/no-effect contract. Running deferral must still stop the active nested branch, produce no further effects or duplicate cleanup across repeated suspended ticks, retain the behavior/node without disposal, then restart a fresh branch lifetime when deferral clears. Those stronger effect/cleanup/resumption assertions still test the intended defect without rewriting any original assertion. Protective selector scheduling is a separate root-owner responsibility; it must not be solved by breaking the generic deferred iterator contract. This amendment supersedes the terminal-deferral wording in W42_EXECUTOR_OWNERSHIP_PLAN.md.

The source also resolves the likely rescan cancellation boundary: QuestScheduler.AssessNavigation and CreateCachedNavigationAssessment each contain broad safety/navigation catch blocks. Add the actual16-case QuestNavigationCancellationRegressionTests group before changing them. Its provider callbacks are counted, both cancellation signal types are checked at direct/cache/actual-refresh boundaries, ordinary errors remain conservative unknown with existing once-per-scan caching, and late cancelled scans must preserve a genuine reentrant replacement publication. No test modifies a production method or invokes native navigation.

At this document's creation, these refined/new tests have not executed and no production repair has been made. Require a new exact-source red before implementing the bounded executor/Composite/navigation cancellation repair. All original fixtures, historical matrix arms, source identities and protected branch constraints remain unchanged.
