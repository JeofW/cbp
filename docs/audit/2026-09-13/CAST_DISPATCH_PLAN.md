# Shared cast-dispatch result ownership

Owner-approved continuation of the shared Spell.Cast audit; combined source baseline fee36e5c6dd1fc09c9aae41aba5092daf03791ce. The name and ID dispatch actions ignore SpellManager.Cast's bool result, so the void TreeSharp.Action constructor unconditionally reports Success. They also call the target selector separately for logging and dispatch, after earlier setup may have yielded. Buff's success-only bookkeeping follows this action, and a priority selector cannot reach movement after false Success.

Bounded repair: retain all public signatures and existing setup/priority order. At the actual dispatch action, reject invalid name/ID, missing selector and missing target before logging. Resolve the target once for that dispatch and reuse it. Return RunStatus.Success only when SpellManager.Cast returns true; otherwise return Failure. Do not add arbitrary retry timers or suppress unrelated movement. Do not equate the backend's true result with server-confirmed cast acceptance.

Tests first execute the actual source-compiled dispatch action found through factory IL, preserving its existing delegate return type. Only client-dependent factory construction is bypassed. The real SpellManager is invoked with an unknown spell; no fake cast implementation is used. Cases cover name/ID rejection, disappearing target, missing selector, invalid identifiers, selector cardinality, priority fallback, success-only bookkeeping and pre-existing prevention state. Capture exception logging because TreeSharp converts exceptions to Failure. Red execution must establish these failures before production changes. Run the repaired tests and all ten combined entries afterward.

Remaining boundary: target identity across the entire multi-tick setup, actual live cast acceptance, native executor cancellation and the older public void cast dispatch semantics are not solved by this action repair. Retain those gates rather than overstating an action-level regression as a full combat execution test.
