# Retribution availability and owner-boundary repair

Continue the owner-approved audit on top of 67ce37a0a1a902ccbffa2ff873cd7b05d04e2207; no merge or deployment. This slice tests concrete source gates rather than claiming an optimal WotLK rotation.

Baseline: all three active Retribution combat factories suppress Divine Storm below four nearby targets and suppress Crusader Strike above that count when Divine Storm is merely known, regardless of its actual availability. Two offensive factories also register as Heal, competing with the dedicated healing factory. All three shield predicates use !HordeFlag OR !AllianceFlag, permitting the action when exactly one exclusion is present. Battleground self-buff predicates dereference a disappearing CurrentTarget even though Spell.BuffSelf's own selected unit is the player.

Test first with the exact linked Retribution source, its attributes and Throttle, and production TreeSharp. Only world observation, native spell readiness/dispatch, and unrelated movement/target helpers are boundary fixtures. This is stronger than testing a rewritten priority list but is not full client-bound composition, true spell-range/CC safety verification, a spell learning table, or live DPS measurement. Assertions include chosen/rejected traces, unknown/unavailable spells, 1–5 target counts, controlled level-33/80 availability, current flags, target loss, and no swallowed exceptions. The current Exorcism policy and compatibility assertions are retained; its opportunity-cost redesign remains separate.

After observing expected failures, keep CS then DS ordering but make the independent melee choices available through one shared factory, relying on the existing cast helper's actual spellbook/range/cooldown/area-safety checks. Remove offensive Heal registration, preserve the dedicated healer, correct the two-flag Boolean veto and guard target-dependent battleground predicates. Do not reorder defensives, change seals globally, widen AoE safety, or silently remove existing compatibility checks. Run real full Singular compilation after structural changes and replace a brittle lambda-number check only with a semantically anchored equivalent regression, never by deleting the post-kill assertion.

Each source change needs the failing-before and passing-after result, exact source hashes, focused and combined tests, graph/PR traceability and explicit live acceptance gates.
